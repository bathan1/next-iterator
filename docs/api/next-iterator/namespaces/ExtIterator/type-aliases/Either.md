---
title: Either
description: |-
  `Either<L, R>` is values of types `L` boxed in a `"left"` disciminateable
  object and `R` boxed in `"right"` kinds.
---

# Type Alias: Either\<L, R\>

> **Either**\<`L`, `R`\> = \{ `kind`: `"left"`; `value`: `L`; \} \| \{ `kind`: `"right"`; `value`: `R`; \}

Defined in: [ext-iterator/types.ts:9](https://github.com/bathan1/utop.js/blob/d0b816dc2e537165a668751d1c76e9c28d32dc96/src/ext-iterator/types.ts#L9)

`Either<L, R>` is values of types `L` boxed in a `"left"` disciminateable
object and `R` boxed in `"right"` kinds.

## Type Parameters

### L

`L`

### R

`R`
