---
title: Either
description: |-
  `Either<L, R>` is values of types `L` boxed in a `"left"` disciminateable
  object and `R` boxed in `"right"` kinds.
---

# Type Alias: Either\<L, R\>

> **Either**\<`L`, `R`\> = \{ `kind`: `"left"`; `value`: `L`; \} \| \{ `kind`: `"right"`; `value`: `R`; \}

Defined in: [ext-iterator/types.ts:9](https://github.com/bathan1/utop.js/blob/bdc3f98b8e61139cc46ddf4c6d96ebfa7966f602/src/ext-iterator/types.ts#L9)

`Either<L, R>` is values of types `L` boxed in a `"left"` disciminateable
object and `R` boxed in `"right"` kinds.

## Type Parameters

### L

`L`

### R

`R`
